const Events = {
    CONNECT: "connect",
    USER_CONNECTED: "user:connected",
    USER_DISCONNECTED: "user:disconnected",
    MESSAGE: "message",
    USER_ID: "user:id",
};

module.exports = { Events };
